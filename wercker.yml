box: wercker/default
deploy:
  steps:
    -  add-to-known_hosts:
         hostname: $SERVER_HOSTNAME
    -  add-ssh-key:
         keyname: GCE
    -  script:
         name: Remove symlink
         code: ssh deployer@$SERVER_HOSTNAME 'bash -s' < 'sudo docker restart goapi'